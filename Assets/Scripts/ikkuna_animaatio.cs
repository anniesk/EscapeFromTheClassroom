using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ikkuna_animaatio : MonoBehaviour
{
    public Animator ikkuna;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        /*float timerAika;
        if(timerAika > 10000){
            ikkuna.SetFloat("time", 5);
        }
        else if (timerAika > 1000){
            ikkuna.SetFloat("time", 4);
        }
        else if (timerAika > 100){
            ikkuna.SetFloat("time", 3);
        }
        else if (timerAika > 10){
            ikkuna.SetFloat("time", 2)
        }*/
    }
}
